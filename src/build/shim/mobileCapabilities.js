// Stub for lightning/mobileCapabilities — not available outside the Salesforce platform.
// Returns null so BarcodeScanner falls back gracefully (isAvailable() → false).
export function getBarcodeScanner() {
    return null;
}
